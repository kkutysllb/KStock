{
  "name": "kstock",
  "private": true,
  "version": "0.1.3",
  "packageManager": "pnpm@9.15.0",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "test:sidecar": "python -m pytest sidecar/tests -q",
    "dev:desktop": "node scripts/run_with_log.mjs desktop -- pnpm -C apps/desktop electron:dev",
    "dev:web": "node scripts/run_with_log.mjs frontend -- pnpm -C apps/desktop dev",
    "test:desktop": "pnpm -C apps/desktop test",
    "build:web": "pnpm -C apps/desktop build",
    "build:desktop": "bash scripts/build-desktop.sh"
  }
}
