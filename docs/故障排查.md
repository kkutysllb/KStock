# 故障排查

## 1. `pytest` 找不到包

先确认已经安装侧车依赖：

```bash
python -m pip install -e ./sidecar
```

## 2. `pnpm test` 报依赖缺失

重新安装前端依赖：

```bash
pnpm install
```

## 3. Playwright 提示没有浏览器

安装 chromium：

```bash
pnpm -C apps/desktop exec playwright install chromium
```

## 4. 技能包校验失败

重新同步精选技能：

```bash
python scripts/sync_upstreams.py --sync-skills
python scripts/verify_skill_pack.py
```

