# 运行说明

## 本地开发

```bash
pnpm install
python -m pip install -e ./sidecar
pnpm -C apps/desktop dev
```

## 运行测试

```bash
python -m pytest sidecar/tests -q
pnpm -C apps/desktop test
pnpm -C apps/desktop exec playwright test
```

## 构建产物

- `dist/kstock-sidecar.pyz`
- `apps/desktop/dist/`

