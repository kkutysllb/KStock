# 上游同步说明

## 本地源仓库

- QiLin：`/Users/libing/kk_Projects/QiLin`
- KSkills：`/Users/libing/kk_Projects/KSkills`

## 同步命令

```bash
python scripts/sync_upstreams.py --refresh-lock
python scripts/sync_upstreams.py --sync-qilin
python scripts/sync_upstreams.py --sync-skills
```

## 校验命令

```bash
python scripts/verify_skill_pack.py
```

## 规则

- 只同步批准清单里的技能
- QiLin 同步到 `vendor/qilin`，作为产品发布输入
- 不直接修改上游仓库
- 所有产品定制留在 KStock
