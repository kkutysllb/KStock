# 任务计划

## 目标
把 KStock 重做成一个跨平台桌面端 Stock Quant Agent，核心引擎用 QiLin，技能体系只引入适合“研究 / 分析 / 报告”的精选技能，并且能持续跟踪 QiLin / KSkills 的上游更新而不污染上游仓库。

## 阶段

1. [已完成] 了解当前仓库和上游上下文。
2. [已完成] 定义产品架构、桌面技术栈和同步策略。
3. [已完成] 筛选 KSkills 子集并定义技能加载规则。
4. [已完成] 编写设计稿并自检是否有冲突或遗漏。
5. [已完成] 等用户确认后进入实施计划。
6. [已完成] 实施任务 1，建立仓库骨架与基础工具链。
7. [已完成] 实施任务 2，打通 QiLin sidecar 协议与桥接层。
8. [已完成] 实施任务 3，落地精选技能包与上游同步机制。
9. [已完成] 实施任务 4，实现桌面工作台与对话工作流。
10. [已完成] 实施任务 5，打通持续集成与发布流水线。
11. [已完成] 实施任务 6，补齐中文运行文档与首次使用说明。
12. [已完成] 设计用户数据空间组织，和 QiLin 引擎的用户、线程、sandbox、数据库机制对齐。
13. [已完成] 用户审阅 `docs/用户数据空间组织设计.md`，确认后进入实施计划。
14. [已完成] 审阅并执行 `docs/superpowers/plans/2026-07-31-user-data-space.md`，完成用户数据空间实施。
15. [已完成] 全量验证用户数据空间、桌面端协议类型、Tauri app data 命令和 sidecar 打包入口。

## 约束

- 桌面端覆盖 macOS / Windows / Linux。
- QiLin 作为本地核心引擎。
- KSkills 只取精选技能，不整包搬入。
- 本地产品要跟踪上游版本，但不能反向污染上游。
- 产品体验以对话为中心，像 Codex 一样完成量化任务。
- 用户数据空间必须适合正式桌面打包后的跨平台应用数据目录，不能依赖仓库内 `.kstock` 作为最终运行目录。

## 当前结论

- 正式桌面端默认使用跨平台系统应用数据目录，不默认使用 `~/.kstock`。
- `~/.kstock` 仅作为高级用户显式 `KSTOCK_APP_DATA_DIR=~/.kstock` 覆盖或开发 fallback。
- KStock 采用“QiLin 运行真源 + KStock 产品索引层”：QiLin 的用户、线程、sandbox、SQLite 继续由 QiLin 管理，KStock 只维护项目、报告、标签、最近项和 UI 状态索引。
